name: 'risco_credito_fintech'
version: '1.0.0'
config-version: 2

profile: 'risco_credito'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  risco_credito_fintech:
    staging:
      +materialized: view
    marts:
      +materialized: table
